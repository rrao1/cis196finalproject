<p id="notice"><%= notice %></p>

<p>
  <strong>Ingredient:</strong>
  <%= @search.keyword %>
</p>
<% plural = @search.keyword.pluralize(2) %>
<% counter = 0 %>
<% Recipe.all.each do |recipe| %>
	
	<% if recipe.ingredients.include?(@search.keyword) || recipe.ingredients.include?(plural)%>
	<% counter += 1 %>
	<%= link_to "#{recipe.name}", "/recipes/#{recipe.id}" %><br>
	<% end %>
<% end %>
<% if counter == 0 %>
	<%= "No recipes containing that ingredient!" %>
	<% end %>

<%= link_to 'Edit', edit_search_path(@search) %> |
<%= link_to 'Back', searches_path %>
